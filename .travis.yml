sudo: required

services:
  - docker

stages:
  - build

before_script:
  - whoami
  - docker version
  - cat /proc/version

script:
  - date 
  - docker build -t ubuntu:test build/ --build-arg IMAGE=ubuntu:16.04
  - docker run ubuntu:test /bin/sh -c "steamcmd.sh +login anonymous +force_install_dir /app +app_update 90 validate +quit"
  - docker images

  - date

after_script:
  - date
